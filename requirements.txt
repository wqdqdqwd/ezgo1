# Ultra stable versions for Python 3.13 compatibility
fastapi==0.68.0
uvicorn==0.15.0
gunicorn==20.1.0
python-multipart==0.0.5
python-dotenv==0.19.0
pydantic==1.8.2

# Firebase (essential) 
firebase-admin==5.4.0

# Binance API (essential) - includes WebSocket functionality
python-binance==1.0.15

# Security & Crypto (minimal version)
cryptography==3.4.8

# HTTP client (use requests only - no aiohttp)
requests==2.28.0

# Basic dependencies only
typing-extensions==3.10.0
