# Minimal FastAPI setup for Render.com compatibility
fastapi==0.100.1
uvicorn[standard]==0.23.2
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic==1.10.12

# Firebase (essential)
firebase-admin==6.2.0

# Binance API (essential)
python-binance==1.0.19

# Security & Crypto (minimal version)
cryptography==40.0.2

# HTTP client (use requests only - no aiohttp)
requests==2.31.0

# Basic dependencies only
typing-extensions==4.7.1
