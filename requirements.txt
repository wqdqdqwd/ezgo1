# Flask-based minimal setup (no FastAPI/Pydantic issues)
flask==2.3.3
flask-cors==4.0.0
gunicorn==20.1.0
python-dotenv==0.19.0

# Firebase (essential) 
firebase-admin==5.4.0

# Binance API (essential)
python-binance==1.0.15

# Security & Crypto (minimal version)
cryptography==3.4.8

# HTTP client
requests==2.28.0

# Basic dependencies
typing-extensions==3.10.0
